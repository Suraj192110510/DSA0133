#include<iostream>
using namespace std;
int main()
{
	string str,rts;
	int a,f=0;
	cout<<"Enter the String : ";
	cin>>str;
	a=str.length();
	for(int i=1;i<=a;i++)
	{
		if (str[i] != str[a-i-1])
		f++;
	}
	if(f!=0)
	cout<<str<<" is not palindrome";
	else
	cout<<str<<" is palindrome";
}
